Convert the memory store to a content-hash-keyed md corpus under .gm/memories/. Boot the spool (plugkit >= 0.1.824 has automatic conversion), let the engine convert flat timestamp-keyed memory files to content-hash-keyed md, then commit and push to main as ONE commit.