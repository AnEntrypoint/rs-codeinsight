Fix 6 audit issues in this crate (code indexer, sync-before-emit contract): 1) README.md falsely claims rs-plugkit uses rs_codeinsight::collect_files -- verify no repo depends on this crate as live Cargo dependency, correct README. 2) src/lib.rs ~201-203 oversized-file skip (max_file_size 200KB) not recorded in skipped_files, and possible inverted metadata() error guard including oversized file instead of excluding -- fix both. 3) src/lib.rs ~247-250 compute_freshness_digest_from_files (dead code, zero callers) hashes literal "__unreadable__" causing non-deterministic/indistinguishable digests for unreadable files -- fix to path-based deterministic hash or documented exclusion. 4) src/scanner.rs ~27-173 secrets scanner 14 stacked substring suppressions (test/example/placeholder) -- check for word-boundary matching improvement, fix only if low-risk else leave. 5) src/project.rs ~461-484 unescape_json_string doesn't decode \uXXXX escapes -- fix display bug. 6) .gitignore ignores .code-search/ contradicting tracked-index convention -- check live usage, remove ignore + track dir if applicable. Verify each fix compiles per this crate's own CI workflow invocation. Commit and push to main, watch CI green.