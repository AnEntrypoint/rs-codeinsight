Fix 8 pre-identified adversarially-verified audit findings in rs-codeinsight: (1) HIGH unguarded recursion in count_params::walk, structural_hash::walk, extract_import_path (analyzer.rs) and depgraph.rs cycle-detection DFS - apply same MAX_TRAVERSE_DEPTH guard traverse() has, or convert to iterative. (2) MEDIUM func_hashes HashMap keyed only by name(paramCount) drops same-name/same-arity duplicates within a file - widen key with byte-offset/scope while preserving cross-file duplicate detection. (3) MEDIUM detect_data_layer (models.rs/lib.rs) re-walks filesystem with hardcoded ignore list, ignoring .codeinsight.toml config that collect_files honors - reuse collected file list or honor same config. (4) MEDIUM resolve_go_import/resolve_python_import/resolve_alias_import (depgraph.rs) each rebuild top_dirs/files.keys() scan per-call - hoist once in build_dep_graph, pass as param. (5) LOW UTF-8 decode failures in lib.rs silently dropped via .ok()? - surface as skipped_files list with reason. (6) LOW config.rs parse_string_array naive comma-split corrupts quoted-comma values - use real TOML array type if already TOML, else add quote-awareness. (7) LOW git.rs porcelain status parsing assumes fixed 3-char prefix, mis-parses rename/copy R old -> new lines - parse 2-char status code then split on arrow for renames. (8) LOW formatter.rs File Index display has off-by-one cliff at 30/31 files - degrade smoothly with min(count,LIMIT) plus and N more. For each: codesearch to locate real code, minimal correct fix, witness via real cargo check/build/test or real exec against a pathological fixture. If a finding is already fixed or false positive, resolve with reasoning. Push via git_finalize.