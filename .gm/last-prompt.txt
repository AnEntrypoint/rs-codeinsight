Consolidate all git branches/worktrees in this repo down to just main. List every local branch, remote branch, and worktree. For each non-main worktree: check for unmerged commits vs main, merge if any (ff or real merge, never discard), remove the worktree, delete its local+remote branch. For every other local branch: same treatment. For every remote branch on origin: if real work, checkout+merge+push then delete remote branch; if clearly bot/stale/superseded, may delete directly. Verify main is healthy via cargo check before and after merges. Final state: only main local, only origin/main remote, no extra worktrees, verified via real git verb dispatch.