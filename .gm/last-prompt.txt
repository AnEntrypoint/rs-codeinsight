Fresh constraint-audit applying a formal-logic constraints spec (dependent-typed/no-any, affine/leak-free state, total pure-or-IO no hidden mutation, race/deadlock-free concurrency, idempotent networking, constant-time crypto, complete proofs, cache-local/branch-predictable perf, exhaustive error handling no panics, fully-expanded code no TODO/FIXME/phantom stubs, parameterized/injection-free auth, output matching instruction) to this Rust crate (rs-codeinsight), plus file-level cleanup (keep only what we need). Prior session already fixed: recursion guards on count_params::walk/structural_hash::walk/extract_import_path/depgraph dfs; widened func_hashes dedup key; made detect_data_layer honor .codeinsight.toml; hoisted import-resolver per-call loops in depgraph.rs; surfaced non-UTF8 file skips; quote-aware parse_string_array; git.rs porcelain rename-parsing fix; formatter.rs File Index 30/31 display cliff fix; plus a bonus compile-blocking duplicate-definition fix in lib.rs. Committed through 9f11d50, CI green -- do NOT re-investigate any of that.

Scope for THIS session:
1. PERF AUDIT (cache-local + branch-predictable clause): sweep analyzer.rs, depgraph.rs, lib.rs for OTHER quadratic-or-worse algorithmic patterns beyond the already-fixed import-resolver hotspot -- nested loops over the full file/function set repeated per-item instead of hoisted/precomputed. This crate analyzes potentially huge real repos so algorithmic complexity is load-bearing.
2. PANIC AUDIT (exhaustive error handling clause): codesearch for .unwrap()/.expect()/panic!() reachable from caller-controlled parsed source file content (arbitrary, possibly malformed/adversarial source files in many languages). Beyond already-fixed recursion guards, look for: unwrap on regex capture groups that might not match some real edge-case syntax, array/slice indexing assuming a minimum length, integer ops that could overflow/underflow on crafted input, unwrap on file/string ops that can fail on binary-ish/unusual content. For EACH candidate, construct or reason through a real adversarial input before deciding it's worth fixing -- don't fix unreachable internal-invariant cases. Fix genuine ones with proper Result/Option handling, no panics.
3. UNSAFE AUDIT: codesearch for unsafe blocks, verify each is genuinely sound (document why, or fix if not).
4. TODO/STUB AUDIT: codesearch for TODO/FIXME/unimplemented!/todo!/stub markers, or a language-support code path that's partially implemented and silently no-ops on certain constructs instead of erroring/handling correctly.
5. DEAD/DUPLICATE CODE AUDIT: codesearch for any exported function/module with zero real callers; remove or wire up.

Verify every finding via real code reading (never just counting occurrences). Witness fixes via real cargo check/cargo build (Bash allowed for read-only/build, never for search or git-mutation) and where practical run the analyzer against a real pathological fixture file to confirm a fix works. No synthetic test files. Push via git_finalize when done (direct push to main is normal here, no PRs).