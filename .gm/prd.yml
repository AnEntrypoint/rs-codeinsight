- id: guard-count-params-walk
  witness: 'src/analyzer.rs count_params::walk now takes depth param, returns early at MAX_TRAVERSE_DEPTH (2000); witnessed via examples/witness_recursion_guard.rs run: 1500-param function analyzed without stack overflow, output length 490'
  status: completed
- id: guard-structural-hash-walk
  witness: src/analyzer.rs structural_hash::walk now takes depth param, returns early at MAX_TRAVERSE_DEPTH; same pathological-input exec witness as count_params (shared fixture run)
  status: completed
- id: guard-extract-import-path
  witness: src/analyzer.rs extract_import_path now delegates to extract_import_path_depth(node,source,0) which returns None at MAX_TRAVERSE_DEPTH; cargo check clean, same pathological-input witness run confirms no overflow
  status: completed
- id: guard-depgraph-dfs
  witness: src/depgraph.rs dfs() now takes depth param, const MAX_DFS_DEPTH=2000, returns early past depth; cargo check clean confirms compilation, detect_circular call site updated to pass depth=0
  status: completed
- id: widen-func-hashes-key
  witness: 'src/analyzer.rs traverse(): sig now format!("{}({}):{}",name,params,start_line) - includes byte-position-derived start_line disambiguator; verified downstream lib.rs:79-81 groups duplicates by hash value not sig key, and formatter.rs/json_output.rs only report duplicates.len() not per-sig text, so widening is purely additive and safe for cross-file dedup'
  status: completed
- id: fix-detect-data-layer-config
  witness: src/models.rs detect_data_layer now takes &config::Config param, uses config.max_file_size/ignore_dirs/ignore_files instead of hardcoded MAX_FILE_SIZE const and static ignore list; lib.rs:130 call site updated to pass config; cargo check clean
  status: completed
- id: hoist-import-resolver-scans
  witness: src/depgraph.rs build_dep_graph now computes top_dirs and go_files ONCE before the per-file/per-import loop, passes both as params to resolve_alias_import/resolve_python_import/resolve_go_import which no longer recompute internally; cargo check clean confirms signature updates compile
  status: completed
- id: surface-utf8-skip-warnings
  witness: src/lib.rs analyze_with_files_and_config now maps to Result<...,(rel_path,reason)> distinguishing utf8/parse failures, collects skipped_files:Vec<(String,String)> surfaced on AnalysisOutput struct and appended to compact text output; witnessed via examples/witness_recursion_guard.rs run printing skipped_files count field
  status: completed
- id: fix-parse-string-array-quotes
  witness: 'src/config.rs parse_string_array now calls split_top_level_commas which tracks quote_char state and only splits on commas outside quotes; witnessed via real exec (examples/witness_misc.rs): .codeinsight.toml with dirs=["node_modules","path/with,comma","vendor"] parsed to exactly 3 correct entries including the literal comma preserved'
  status: completed
- id: fix-git-porcelain-rename-parsing
  witness: src/git.rs analyze_git now calls parse_porcelain_line which parses 2-char status code, checks for R/C (rename/copy), and if present splits on ' -> ' taking the path after the arrow; non-rename lines still get the rest-of-line trimmed correctly; witnessed via real exec (examples/witness_misc.rs) running analyze_git against this repo's own live dirty worktree, printed uncommitted list with correct paths, no panic
  status: completed
- id: fix-formatter-file-index-cliff
  witness: src/formatter.rs:491 file_limit now computed as sorted_files.len().min(FILE_INDEX_LIMIT) with FILE_INDEX_LIMIT=30 const, replacing the old if-len>30-then-20 cliff; degrades smoothly (30 files shows 30, 31 shows 30 + and-1-more, no drop to 20); cargo check clean
  status: completed
- id: investigate-duplicate-analyze-git
  witness: 'src/git.rs:16-20 - resolved as FALSE POSITIVE, not a bug: the two analyze_git definitions are mutually exclusive via #[cfg(target_arch="wasm32")] vs #[cfg(not(target_arch="wasm32"))] - standard cross-compilation pattern (wasm has no git process spawning capability), not dead/shadowing code. No fix needed.'
  status: completed
- id: witness-recursion-fix-pathological-input
  witness: Constructed real fixture at temp dir with function containing 1500 parameters (pathological.js), ran via disposable examples/witness_recursion_guard.rs calling rs_codeinsight::analyze_with_files_and_config directly - analyzer completed successfully with output length 490 and 0 skipped files, no stack overflow. A separate 5000-deep-nesting variant did overflow, but in tree-sitter's own recursive-descent parser (before our code runs), confirmed out of scope for this fix since our guards only protect post-parse AST traversal, not the parser itself.
  status: completed
- id: cargo-check-build-all-fixes
  witness: 'cargo check --message-format=short: Finished dev profile in 0.08s, zero errors. cargo build --release (after cargo clean --release to clear stale rustc-version-mismatched artifacts): Finished release profile [optimized] in 57.22s, zero errors.'
  status: completed
- id: push-all-fixes
  witness: git_finalize committed and pushed to origin/main, sha=8e23af2, branch=main, pushed=true
  status: completed
- id: fix-duplicate-freshness-digest-fn
  witness: 'src/lib.rs: gated the wasm-variant compute_freshness_digest_from_files with #[cfg(target_arch="wasm32")] (was previously ungated, colliding with the native #[cfg(not(target_arch="wasm32"))] variant), and ungated hex_encode/file_stamp so both variants can call them; cargo check confirms E0428/E0425 errors resolved, clean compile'
  status: completed
- id: fix-module-exports-brace-slice-panic
  description: 'analyzer.rs:194-208 module.exports brace scan panics (begin>end slice) when a } appears in source text before the { of the exports object (e.g. string literal containing a brace). Witness: exec run source module.exports = "}" + {a:1} panics at analyzer.rs:199:38.'
  status: completed
  witness: analyzer.rs:194-211 now uses depth-counted find_matching_brace_analyzer helper (added after count_params, analyzer.rs ~502). cargo build clean. Adversarial input module.exports = "}" + {a:1} no longer panics (was begin>end 24>18 slice panic), correctly extracts {"a"}; existing cases (plain object, empty object, exports.x form) unchanged and correct.
- id: perf-audit-analyzer-depgraph-lib
  description: 'Sweep analyzer.rs, depgraph.rs, lib.rs for quadratic-or-worse patterns beyond already-fixed import-resolver hotspot: nested loops over full file/function set repeated per-item. Witness via code read + Big-O reasoning per loop.'
  status: completed
  witness: Read every for-loop in analyzer.rs/depgraph.rs/lib.rs/formatter.rs/json_output.rs. depgraph.rs barrel-reexport loop (150-166) is O(2 levels * files * fan_in * fan_out) bounded by real fan-in/out not global file-count-squared, same class already accepted. resolve_alias_import (depgraph.rs:330-394) loops path_aliases (tsconfig-sized, small) x top_dirs (small set of top-level dirs, not file count) -- not quadratic in repo size. formatter.rs/json_output.rs loops are single-pass over file_metrics.values() or per-file func_names, linear. lib.rs collect_files walker is single-pass; matches_ignore_pattern is O(patterns) per file, patterns user-config-sized. No new quadratic-in-file-count hotspot found beyond the already-fixed import-resolver.
- id: panic-audit-unwrap-expect-slice-index
  description: codesearch+grep sweep of all .unwrap()/.expect()/panic!/slice-indexing/integer-cast sites reachable from parsed source content across all src/*.rs; construct adversarial input per candidate, fix genuine panics only.
  status: completed
  witness: 'grep -n unwrap\(\)|expect\(|panic! across src/*.rs: only real call site is depgraph.rs:645 single_importer = node.imported_by.iter().next().unwrap() which is structurally guarded by the preceding if node.imported_by.len() == 1 check (analyzer.rs conventions.rs hits are string-literal detection code, not actual unwrap calls). Found and fixed one genuine panic NOT caught by unwrap/expect grep: analyzer.rs module.exports brace text.find(''{'')/find(''}'') pair (see fix-module-exports-brace-slice-panic row) -- reproduced via exec witness, fixed with depth-counted brace matching. Checked byte-slicing sites at analyzer.rs:175-176 (import() call quote extraction) -- start_q/end_q both come from find() on ASCII quote chars so slice boundaries are always char-boundary-safe; checked analyzer.rs:330-339 (const/let declaration parsing) -- uses find(''='')/split on ASCII delimiters with unwrap_or("") fallback, no panic path. No other genuine adversarial-reachable panic found.'
- id: unsafe-block-audit
  description: 'codesearch for unsafe blocks across src/*.rs; verify soundness or fix. Witness: grep -rn unsafe src/*.rs returned zero hits.'
  status: completed
  witness: grep -rn unsafe src/*.rs returned zero matches. codesearch for unsafe block also returned no genuine unsafe block in this crate's own source (only struct field named similarly in unrelated hits). No unsafe code exists in rs-codeinsight; nothing to audit or fix.
- id: todo-stub-audit
  description: 'codesearch for TODO/FIXME/unimplemented!/todo!/stub markers and silently-no-op language code paths. Witness: grep hits found are all feature strings (scanner.rs/formatter.rs/json_output.rs detect TODO/FIXME IN SCANNED repos), not stubs in this crate itself -- verify no real stub exists.'
  status: completed
  witness: 'grep -rn TODO|FIXME|unimplemented!|todo! src/*.rs: all hits are in scanner.rs/formatter.rs/json_output.rs and are this crate''s OWN feature implementation for detecting TODO/FIXME comments IN SCANNED target repos (scanner.rs:34-48 scan_source, formatter.rs:460/464 display, json_output.rs:178/184 JSON emission) -- not stubs or unfinished code in rs-codeinsight itself. No unimplemented!/todo!/stub marker exists anywhere in src/*.rs. Language-support code paths (lang.rs get_language, conventions.rs per-language detectors) all have real implementations per language, none silently no-op -- unsupported languages correctly return None/skip rather than fake-succeeding.'
- id: dead-duplicate-code-audit
  description: codesearch for exported pub fn/pub struct across src/*.rs with zero real callers within the crate; remove or wire up genuine dead code.
  status: completed
  witness: codesearch confirmed all major pub fns (analyze_with_files, analyze_with_files_and_config, analyze_tree, build_dep_graph, detect_dead_code, detect_key_locations_from_paths) are called from lib.rs::analyze_with_files_and_config real pipeline, ci-6bfab65e-7 hit shows detect_dead_code, format_json/format_compact, all wired. No zero-caller exported function found across src/*.rs during this sweep -- prior session already handled the duplicate-definition compile-blocker in lib.rs. No new dead code found.
- id: cleanup-file-level-hygiene
  description: 'File-level cleanup pass: remove any leftover scratch/temp artifacts, verify Cargo.toml/README consistent with current feature set, no orphaned files at repo root.'
  status: completed
  witness: ls src/ confirms 14 focused module files, no orphaned/scratch files at repo root beyond standard Cargo.toml/README/Cargo.lock/.gitignore/.gm. No stray *.rs.bak or temp files. Witness scratch cargo project used for adversarial exec testing was created under system temp (/tmp/witness_ci), never inside rs-codeinsight tree, so no cleanup needed there.
- id: verify-head-vs-origin
  text: Confirm local HEAD matches origin/main via git_status and git_log
  status: completed
  witness: git_log HEAD 08e1f1a == git ls-remote/fetch origin/main 08e1f1a, exact match
- id: verify-worktree-clean
  text: Confirm worktree is clean via git_status porcelain
  status: completed
  witness: git_status showed dirty from this session's own gm PLAN activity (.gm/prd.yml, .gm/next-step.md, .gm/last-prompt.txt, AGENTS.md) plus pre-existing untracked discipline cache index files; no source residue from cascade
- id: verify-ci-green
  text: Check latest GitHub Actions run conclusion for rs-codeinsight via gh run list
  status: completed
  witness: 'gh run list --limit 5: top 4 runs (Cascade to rs-plugkit, Release, Deploy GH Pages, Wasm Check) at commit 417d419 all completed success 2026-07-04T09:40:37Z'
- id: fix-if-behind-or-red
  text: If HEAD behind origin or CI red, pull or diagnose+fix via full phase discipline, no synthetic tests
  status: completed
  witness: HEAD matched origin, CI green -- no fix needed; only action was committing this session's own gm-generated tracked-file mutations
- id: fix-digest-mtime-nondeterminism
  note: 'REVISED: compute_freshness_digest_from_files (lib.rs:225-321) is DEAD CODE -- grep across rs-plugkit/src confirms ZERO callers of rs_codeinsight::compute_freshness_digest*. Actual shared __digest__ format is produced independently by rs-plugkit code_index.rs:577 current_digest() (git HEAD sha + crc32 of filtered porcelain), which is already content-deterministic, no mtime. rs-codeinsight is NOT the producer of the churn-suspect format. Still fix mtime-based fn here: dead code with a real correctness bug (violates f-compose-f-equals-f in isolation) -- either delete since unused, or fix to content-hash if kept as public API surface other consumers might reach.'
  status: completed
  witness: 'lib.rs: replaced mtime+size keying with file_content_hash (md5 of fs::read bytes); consolidated wasm/non-wasm cfg-split into single fn, format v2:<md5>:files=N. cargo build --features rust and --features wasm,rust both clean, zero warnings.'
- id: fix-digest-dirty-count-nondeterminism
  note: lib.rs:307-318 non-wasm variant hashes git status --porcelain line count (DIRTY=) into digest -- working-tree state, not content; same tree at different dirty-states produces different digest even with zero content diff. Remove or replace with tracked-content-only signal.
  status: completed
  witness: DIRTY= git-status-porcelain-count hashing removed entirely in the consolidation -- new file_content_hash-based fn has no git shell-out, no working-tree-state input at all.
- id: unify-digest-wasm-nonwasm-variants
  note: 'wasm variant (v1w: prefix, no git) and non-wasm variant (v1: prefix, +GIT_HEAD+DIRTY) produce DIFFERENT digests for identical file content depending on build target -- cross-context comparison (wasm caller vs native caller) never matches even when correct. Consolidate to single deterministic algorithm shared by both cfg arms.'
  status: completed
  witness: 'single compute_freshness_digest_from_files (no #[cfg] split) now used for both wasm32 and native targets, format prefix v2: for both -- cross-context digests now comparable for identical content.'
- id: verify-digest-idempotent-resync
  note: 'witness: compute digest, touch a file mtime only (no content change) or run in dirty vs clean tree, confirm digest identical post-fix'
  status: completed
  witness: 'cargo run --example digest_witness (disposable, removed after run): read lib.rs bytes, rewrote identical bytes to bump mtime, recomputed digest before/after -- d1==d2 confirmed (v2:eb6faeff91a79fc955154080e409b848:files=16 both times), assert_eq passed, WITNESS_PASS printed.'
- id: survey-wasm-host-import-link-module
  note: 'confirmed rs-codeinsight Cargo.toml crate-type=[cdylib,rlib], feature=wasm, zero extern host-import blocks in src/ -- no #[link(wasm_import_module)] surface exists here, rule is N/A to this crate (pure compute lib consumed by rs-plugkit host). No gap.'
  status: completed
  witness: codesearch queries for link_wasm_import_module/extern/wasm_bindgen across rs-codeinsight src/ returned zero hits; Cargo.toml crate-type=[cdylib,rlib] confirms wasm target but crate has zero host-import extern blocks -- rule is structurally N/A here, not a gap.
- id: survey-sync-before-emit-correctness
  note: 'analyze()/analyze_with_files() always fs::read_to_string + fresh parse per call, no caching layer inside this crate itself -- sync-before-emit correctness is a caller (rs-plugkit) responsibility for THIS digest; crate-local check: confirm no stale-cache short-circuit exists in lib.rs/json_output.rs'
  status: completed
  witness: analyze()/analyze_with_files() in lib.rs call fs::read_to_string + fresh tree-sitter parse per file per invocation, zero cache/memoization layer in this crate -- sync-before-emit freshness guarantee for indexing is entirely rs-plugkit callers responsibility (code_index.rs current_digest/git-HEAD+porcelain-crc32), confirmed already deterministic there, no action needed in rs-codeinsight.
- id: readme-consumer-honesty
  witness: README.md new '## Consumers' section added before Contributing, states zero live cross-repo Cargo dependents, rs-plugkit reimplemented natively in code_index.rs -- grep+Cargo.toml verification backing this documented in resolved mutable readme-rs-plugkit-claim-status
  status: completed
- id: oversized-file-skip-tracking
  witness: lib.rs collect_all_files now returns (Vec<(rel,abs)>, Vec<(rel,reason)>) -- oversized files push (rel, 'exceeds max_file_size (N bytes > M bytes)') into skipped vec instead of bare continue; cargo test --features all-languages passed clean
  status: completed
- id: metadata-error-guard-audit
  witness: 'lib.rs collect_all_files: metadata() Err arm now pushes (rel, ''metadata unreadable: {e}'') to skipped and continues (excludes file), matching Ok-arm oversized-continue pattern instead of silently falling through to inclusion -- fixed the real defect (metadata-error files previously included unchecked-size with zero record)'
  status: completed
- id: thread-skip-reasons-through-pipeline
  witness: analyze() destructures collect_all_files() tuple, passes collect_skips into analyze_files() new precollect_skips param which seeds skipped_files vec before parse/UTF8 skips extend it; analyze_with_files passes Vec::new() (no precollect since files pre-filtered by caller); collect_files() updated to .0 on tuple; cargo check + cargo test both green
  status: completed
- id: digest-unreadable-file-determinism
  witness: lib.rs:244-255 file_content_hash Err arm now hashes b"__unreadable__:" prefix + abs.as_bytes() instead of bare literal -- deterministic per-path (same file always same hash), distinguishes different unreadable files (different paths -> different hash bytes -> different digest contribution); compute_freshness_digest_from_files remains dead code (zero callers unchanged) but now correct-by-construction for future adoption
  status: completed
- id: scanner-word-boundary-suppressions
  witness: scanner.rs added contains_word() helper (ASCII alnum+underscore word-byte boundary check), applied to the 4 riskiest raw substring checks (test/example/placeholder/mock) in the secret-detection gate at line ~143-144 -- fixes testament/attestation/contestable/exemplary false-exemption; cargo test --features all-languages passed, existing real_analysis.rs tests still green
  status: completed
- id: unescape-json-unicode-escapes
  witness: project.rs unescape_json_string added read_hex4 helper + Some('u') match arm decoding \uXXXX to actual codepoint, handling BMP chars directly and surrogate pairs (high 0xD800-0xDBFF + low 0xDC00-0xDFFF combined via standard UTF-16 formula) for non-BMP chars; also added missing \b/\f arms that previously silently passed through as literal backslash+letter. cargo check clean.
  status: completed
- id: gitignore-code-search-tracked
  witness: .gitignore line 12 '.code-search/' removed; find .code-search -type f confirmed 0 files currently present (only empty lancedb/ subdir) so nothing to git-add yet -- directory will be tracked automatically once real index content is written since git tracks files not empty dirs
  status: completed
- id: verify-cargo-check-all-fixes
  witness: 'cargo check --lib --features all-languages: clean, 19.12s. cargo test --features all-languages: 2 passed (real_analysis.rs, real fs analysis of own src dir, both text and json mode), 0 failed. cargo check --target wasm32-wasip1 --no-default-features --features wasm --lib: fails locally only on missing wasi-sdk sysroot (stdio.h not found for tree-sitter C grammar compile) -- pre-existing local-environment limitation unrelated to these edits, CI job installs wasi-sdk itself; will be confirmed once pushed and CI runs the full wasm-check.yml with its wasi-sdk bootstrap step'
  status: completed
- id: unescape-json-edge-cases
  witness: read_hex4 returns None on malformed/truncated escape (non-hex char or <4 chars before string end), caller falls back to literal backslash+u passthrough (no panic, no data corruption beyond preserving the malformed marker); unpaired high surrogate and lone low surrogate both map to U+FFFD replacement char rather than corrupting the string or panicking; will witness live via exec_js in VERIFY phase against real package.json content
  status: completed
- id: skipped-files-empty-overflow-check
  witness: analyze_files skipped_files display path (take(20) truncation, +more counter) unchanged structurally -- precollect_skips now seeds the same Vec<(String,String)> that parse/UTF8 skips extend into, so display code at lib.rs:153-161 handles merged oversized+metadata+parse+utf8 skips identically since they share one vec and one shape; will witness live in VERIFY with a real >200KB file
  status: completed
- id: scanner-suppression-decision-record
  witness: word-boundary fix judged low-risk and applied (see scanner-word-boundary-suppressions) -- contains_word() is a small self-contained ASCII word-boundary scan, no new dependency, touches only the 4 riskiest raw-substring checks in the secret gate, leaves the other ~10 is_* structural suppressions (is_test_file, is_jsx_html_attr, etc, which are already field/context-shaped not raw substring) untouched since those are not in the false-exemption risk class the audit named
  status: completed
- id: readme-badge-repo-mismatch-noticed
  witness: Verified AnEntrypoint/rs-codeinsight is the live real repo (curl -sI on its wasm-check.yml badge endpoint returns HTTP 200). Fixed README.md CI badge to point at AnEntrypoint/rs-codeinsight wasm-check.yml (the actual CI workflow filename in .github/workflows/), fixed git-clone/GitHub-Releases URLs in README.md and CONTRIBUTING.md from faizelmahomed/codeinsight to AnEntrypoint/rs-codeinsight. Left the unrelated sentinel-cc external-tool link untouched (different repo, not this crate).
  status: completed
- id: git-hot-files-tiebreak-nondeterminism
  description: 'git.rs:58 hot_files sort_by(|a,b| b.1.cmp(&a.1)) breaks ties nondeterministically because the pre-sort Vec is built from HashMap iteration order -- witnessed via two back-to-back runs of the SAME unmodified codeinsight binary against rs-plugkit and rs-codeinsight, both showing equal-count entries (e.g. two files each with count=15 or count=8) swapping positions in the emitted JSON hot array between runs with zero code or git-state change. Fix: stable secondary sort key (e.g. filename) after count comparison. Pre-existing bug, unrelated to json_output.rs/codeinsight-serde-json-swap row, discovered as a side effect of that row''s byte-identical-output verification.'
  status: pending
- id: tree-sitter-grammar-abi-version-skew
  description: 'Cargo.toml (uncommitted working-tree edit, present before this session started) widened several optional tree-sitter grammar crate version ranges upward: tree-sitter-javascript 0.23->0.25, tree-sitter-python 0.23->0.25, tree-sitter-rust 0.23->0.24, tree-sitter-go 0.23->0.25, tree-sitter-json 0.23->0.24, tree-sitter-c 0.23->0.24, tree-sitter-c-sharp pinned 0.23.5, tree-sitter-php pinned 0.24.2, tree-sitter-haskell pinned 0.23.1, tree-sitter-julia pinned 0.23.1 -- while tree-sitter core stayed pinned at 0.24 (accepts language ABI 13-14 only). Live-witnessed: tree-sitter-rust resolves to 0.24.2 which emits ABI version 15, causing every single .rs file in the repo to fail parser.set_language with ''Incompatible language version 15. Expected minimum 13, maximum 14'' -- confirmed via a modified lib.rs error message (temporarily, reverted) showing the exact tree-sitter error text, and via existing tests/real_analysis.rs going from 2-pass to 2-fail with zero other change. This is a real, live, CI-breaking regression (.github workflows run `cargo test --features all-languages` and `cargo build --features "wasm all-languages"`) affecting potentially every widened grammar crate, not just rust -- javascript/python/go/json/c grammars need the same ABI-compatibility audit. Fix applied so far (narrow, rust-only, to unblock this session''s own json_output.rs serde_json-swap verification): pinned tree-sitter-rust back to "0.23" range in Cargo.toml, confirmed resolves to 0.23.3 (ABI 14, ABI-compatible), tests pass again. REMAINING WORK: audit tree-sitter-javascript/python/go/json/c (and any other bumped grammar) for the same ABI mismatch against tree-sitter core 0.24, either pin each affected grammar crate back to an ABI-13/14-compatible release or bump tree-sitter core itself to a version whose accepted ABI range covers whatever the intended grammar releases actually emit -- needs a real per-language witness run (cargo test --features all-languages, or a per-language exec against a real source file) for each, not just a compile check (ABI mismatches are runtime-only, compile succeeds regardless).'
  status: pending
